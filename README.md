# Dog breed classifier

Dog Breed Classifier is a Python machine learning application that predicts dog breeds from images using a convolutional neural network with transfer learning (MobileNetV2). It was trained on a custom dataset created from videos and manually collected images. The application provides a simple user interface for loading images and displays the top predicted breeds with probabilities.

# How to run

To run this project, just download the newest release.
